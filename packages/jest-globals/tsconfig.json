{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // we don't want `@types/jest` to be referenced
    "types": ["node"],
    "rootDir": "src",
    "outDir": "build"
  },
  "references": [
    {"path": "../expect"},
    {"path": "../jest-environment"},
    {"path": "../jest-types"}
  ]
}
